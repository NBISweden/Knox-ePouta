# -*-sh-*-

echo "Installing NFS utils"
yum -y install nfs-utils

rsync -a {{ env['VAULT'] }}/getusers  /usr/local/sbin/getusers
chown root:root /usr/local/sbin/getusers
chmod 0755 /usr/local/sbin/getusers
